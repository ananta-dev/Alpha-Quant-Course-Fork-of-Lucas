# Generated by JJ and Gemini to use uv - It is not cloned from Lucas's repo
[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "Alpha-Quant-Lucas"
version = "0.1.0"
requires-python = ">=3.11.0"

[tool.setuptools.packages.find]
include = ["Quantreo*"]

[tool.pyright]
venvPath = "."
venv = ".alphaquant-lucas"
extraPaths = [".", "typings"]